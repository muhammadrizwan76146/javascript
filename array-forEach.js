function panggilForEach(){
    var data = ['a','b','c'];
    data.forEach(function(value, index, array){
        console.log(value);
        console.log(index);
        console.log(array);
    })
}

panggilForEach();

/* forEach digunakan untuk perulangan */